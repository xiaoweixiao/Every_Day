#include<iostream>
#include<vector>
using namespace std;

int main()
{
	int n;
	while (cin >> n)
	{
		vector<int> in(n);
		for (int i = 0; i < n; i++)
			cin >> in[i];

		int longest = 0, cur = 0;
		for (int i = 0; i < in.size(); i++)
		{
			if (in[i])
		}
	}
	system("pause");
	return 0;
}
